fastapi>=0.110
uvicorn[standard]>=0.29
pytest>=8.0.0
# The core engine (app/strategy.py, execution.py, feed.py, engine.py, db.py)
# uses ONLY the Python standard library, so `python run_sim.py` works with no
# installs. FastAPI + uvicorn are only needed to serve the dashboard.
